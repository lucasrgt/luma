# Patch Manifests

This directory stores versioned patch manifests.

Use one manifest per Allumeria build. Name them with the game version and MVID
once known, for example:

```text
allumeria-0.14.6-<mvid>.json
```

The included `example-manifest.json` is only a template.
